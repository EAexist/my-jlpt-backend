import { JobCallbackAuthGuard } from './job-callback-auth.guard';

describe('JobCallbackAuthGuard', () => {
  it('should be defined', () => {
    expect(new JobCallbackAuthGuard()).toBeDefined();
  });
});
